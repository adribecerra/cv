/*import { useState } from "react";

export default function Header() {
  const [isDark, setIsDark] = useState(false);

  const handleOnClick = () => {
    document.documentElement.classList.toggle("dark");
    isDark === false ? setIsDark(true): setIsDark(false);
  };

  return (
    <header className="bg-indigo-600 dark:bg-gray-800 text-white p-6 shadow-lg flex justify-between items-center">
      <div>
        <h1 className="text-3xl font-bold">Adrian Becerra</h1>
        <p className="text-lg text-indigo-100 dark:text-gray-300">Fullstack Developer | Java | React</p>
      </div>
      <button onClick={handleOnClick} className="btn">{isDark?"Light Mode 🌞": "Dark Mode 😎"}</button>
    </header>
  );
}*/